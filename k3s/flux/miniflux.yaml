apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: miniflux
  namespace: flux-system
spec:
  interval: 1m
  path: ./k3s/miniflux
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
